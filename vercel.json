{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "installCommand": "pnpm install --shamefully-hoist",
  "buildCommand": "ROLLUP_SKIP_NODEJS_NATIVE=true pnpm run vercel-build"
}
